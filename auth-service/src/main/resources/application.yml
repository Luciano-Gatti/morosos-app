server:
  port: ${AUTH_SERVICE_PORT:8080}

spring:
  application:
    name: auth-service
  profiles:
    default: local
  datasource:
    url: ${AUTH_DB_URL:jdbc:postgresql://localhost:5432/auth_db}
    username: ${AUTH_DB_USERNAME:postgres}
    password: ${AUTH_DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    path: /v3/api-docs

app:
  cors:
    allowed-origins:
      - ${FRONTEND_URL:http://localhost:5173}
      - http://127.0.0.1:5173
  jwt:
    issuer: ${JWT_ISSUER:http://localhost:8080}
    audience: ${JWT_AUDIENCE:morosos-app}
    access-token-minutes: ${JWT_ACCESS_TOKEN_MINUTES:15}
    secret: ${JWT_SECRET:3HugO1JOjAKmVZYTMKO6NqWGqvTJ5xO41wiBWyqnuAnDgwo2RrUVqpOJ4I4kWjzyMfBLTWUjw3UV0VeXkjWOpA}
  seed:
    admin:
      enabled: ${AUTH_SEED_ADMIN_ENABLED:false}
      username: ${AUTH_SEED_ADMIN_USERNAME:admin}
      email: ${AUTH_SEED_ADMIN_EMAIL:admin@local.test}
      password: ${AUTH_SEED_ADMIN_PASSWORD:}
      nombre: ${AUTH_SEED_ADMIN_NOMBRE:Administrador}
      apellido: ${AUTH_SEED_ADMIN_APELLIDO:Local}
