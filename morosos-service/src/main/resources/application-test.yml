spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/morosos_test
    username: morosos_test
    password: morosos_test
  jpa:
    show-sql: false

app:
  security:
    jwt:
      issuer: http://localhost:8080
      audience: gestion-aosc
      secret: test_secret_32_bytes_minimum_value
